function solve(rows, colomns) {
    let matrix = [];
    for (let i = 1; i <= rows*colomns; i++) {
        matrix[0].push(i);
        if (i > colomns && i < 2*rows) {
            
        }
    }
}